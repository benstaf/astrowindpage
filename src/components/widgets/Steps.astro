---
const steps = [
  {
    title: 'Agentic trading, not Q&A',
    description:
      'Models are evaluated inside a realistic multi-agent trading system that makes actual portfolio allocation decisions.'
  },
  {
    title: 'Matched market regimes',
    description:
      'Performance is measured across two similar six-month periods — one inside and one outside the training window.'
  },
  {
    title: 'Alpha decay as the bias signal',
    description:
      'A sharp drop in alpha reveals reliance on memorized future information rather than genuine reasoning.'
  },
  {
    title: 'Quant baselines for calibration',
    description:
      'Momentum, mean-reversion, moving-average, and random strategies provide grounding and sanity checks.'
  }
];
---

<section class="bg-gray-50 py-24">
  <div class="mx-auto max-w-7xl px-6">
    <h2 class="text-4xl font-bold text-center">
      Look-Ahead-Bench: measuring real generalization
    </h2>

    <div class="mt-16 grid gap-10 sm:grid-cols-2 lg:grid-cols-4">
      {steps.map(step => (
        <div>
          <h3 class="text-lg font-semibold">{step.title}</h3>
          <p class="mt-3 text-gray-600">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
