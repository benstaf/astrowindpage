---
const stats = [
  {
    title: '−15 to −22 pp',
    description: 'Alpha decay observed in standard LLMs'
  },
  {
    title: '~0 pp',
    description: 'Alpha decay for PiT-Inference models'
  },
  {
    title: '+500B',
    description: 'Parameters in PiT frontier models (GPT-5-level)'
  },
  {
    title: 'Agentic',
    description: 'Evaluated inside real trading agents'
  }
];
---

<section class="border-y border-gray-200 py-16">
  <div class="mx-auto max-w-7xl px-6">
    <div class="grid grid-cols-2 gap-8 text-center sm:grid-cols-4">
      {stats.map(stat => (
        <div>
          <div class="text-3xl font-bold">{stat.title}</div>
          <div class="mt-2 text-sm text-gray-600">{stat.description}</div>
        </div>
      ))}
    </div>
  </div>
</section>
