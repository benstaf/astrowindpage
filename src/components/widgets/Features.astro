---
const features = [
  {
    title: 'Look-ahead bias is everywhere',
    description:
      'Standard LLMs memorize post-hoc market outcomes from their training data, leading to inflated backtests that collapse in live settings.'
  },
  {
    title: 'Scaling makes it worse',
    description:
      'Larger foundation models develop stronger false priors, causing severe alpha decay once the knowledge cutoff is exceeded.'
  },
  {
    title: 'Backtests must survive the future',
    description:
      'If performance disappears outside the training window, the model is not predictive — it is recalling.'
  }
];
---

<section class="py-24">
  <div class="mx-auto max-w-7xl px-6">
    <h2 class="text-4xl font-bold text-center">
      Why most LLM backtests fail in production
    </h2>

    <div class="mt-16 grid gap-12 sm:grid-cols-3">
      {features.map(feature => (
        <div>
          <h3 class="text-xl font-semibold">{feature.title}</h3>
          <p class="mt-4 text-gray-600">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
